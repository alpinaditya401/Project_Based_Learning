# Arsip praktikum — UNVERIFIED

Folder ini adalah contoh akademik React/Vue/Svelte, bukan runtime AquaSmart aktif. Konfigurasi build/dependency belum diverifikasi, dan contoh auth/API memakai placeholder. Tidak diimpor ke aplikasi PHP/SQLite + SPA dalam `server/` dan `web/`. Keberadaan file contoh tidak menjadi evidence keamanan, integrasi backend, atau fitur yang berjalan.

Untuk memakai contoh ini kelak, buat proyek build terpisah, tentukan dependency, ganti mock dengan kontrak API aktual, lalu uji auth, CSRF, ownership dan runtime. Review aplikasi aktif terdapat di `../REVIEW_REPORT.md`.
