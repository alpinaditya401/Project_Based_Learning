# Arsip praktikum — UNVERIFIED

Contoh Next.js ini tidak diimpor ke runtime aktif PHP/SQLite + SPA (`server/` dan `web/`). Build dan dependency belum diverifikasi; cookie autentikasi/endpoint contoh tidak menggantikan auth server sebenarnya. Jangan menjalankannya sebagai layanan login produksi atau mengutipnya sebagai evidence fitur AquaSmart.

Aktivasi memerlukan proyek terpisah, konfigurasi build, integrasi API/session yang benar, dan pengujian keamanan serta runtime. Status aplikasi aktif: `../REVIEW_REPORT.md`.
